---
# Secrets for SeaClaw Platform
# In production, use sealed-secrets or external-secrets-operator
# For demo, we generate these during deploy.sh
apiVersion: v1
kind: Secret
metadata:
  name: seaclaw-secrets
  namespace: seaclaw-platform
type: Opaque
stringData:
  seazero-bridge-token: "REPLACE_WITH_GENERATED_TOKEN"
