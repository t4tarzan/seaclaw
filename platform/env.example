# ═══════════════════════════════════════════════════════════════
#  SeaClaw Platform — Environment Variables Reference
#  Copy this file to .env and fill in your values.
#  Never commit .env to version control.
# ═══════════════════════════════════════════════════════════════

# ── LLM API Keys (at least one required) ────────────────────────
# Sea-Claw supports multiple providers with automatic fallback.
# OpenRouter is recommended — one key gives access to 100+ models.
OPENROUTER_API_KEY=sk-or-v1-...
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AI...

# ── Default Model ────────────────────────────────────────────────
# Model used when the user doesn't specify one at signup.
# Any OpenRouter-compatible model ID works here.
DEFAULT_MODEL=moonshotai/kimi-k2

# ── Gateway Settings ─────────────────────────────────────────────
# Maximum number of concurrent agent pods allowed on this cluster.
MAX_INSTANCES=5

# Kubernetes namespace where agent pods are created.
SEACLAW_NAMESPACE=seaclaw-platform

# Docker image name for agent pods (must be imported into K3s).
SEACLAW_IMAGE=seaclaw-instance:latest

# Directory inside the gateway pod for persistent data.
DATA_DIR=/data/platform

# Gateway log level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# ── SeaZero Bridge ───────────────────────────────────────────────
# Auto-generated by deploy.sh using openssl rand -hex 32.
# Set manually here if you are not using deploy.sh.
SEAZERO_BRIDGE_TOKEN=REPLACE_WITH_GENERATED_TOKEN

# ── Telegram Channel (per-agent, set at signup) ──────────────────
# These are injected into each agent pod at creation time.
# Users provide them via the signup UI — not set globally here.
# TELEGRAM_BOT_TOKEN=123456:ABC...
# TELEGRAM_CHAT_ID=890034905

# ── Discord Channel (per-agent, set at signup) ───────────────────
# Both vars are required for Discord to activate on an agent.
# DISCORD_BOT_TOKEN=MTxxxxxxx.Gyyyyy.zzzzzz
# DISCORD_CHANNEL_ID=1234567890123456789

# ── Slack Webhook (per-agent, set at signup) ─────────────────────
# Outbound-only. Sea-Claw POSTs responses to this webhook URL.
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx

# ── Voice / Whisper Transcription ────────────────────────────────
# Required for tool #77 (voice_transcribe) to use Groq cloud API.
# Falls back to local whisper-cli if not set.
GROQ_API_KEY=gsk_...
WHISPER_MODEL=whisper-large-v3-turbo

# ── Web Search (optional) ────────────────────────────────────────
# Used by the web_search tool. Exa provides semantic search.
EXA_API_KEY=exa-...

# ── Agent Zero (optional) ────────────────────────────────────────
# LLM provider config for the Agent Zero shim pod.
# Leave blank to run Agent Zero in stub/offline mode.
AGENTZERO_LLM_PROVIDER=openai
AGENTZERO_LLM_MODEL=gpt-4o-mini
AGENTZERO_LLM_API_KEY=sk-...

# ── TLS / Ingress (optional) ─────────────────────────────────────
# Used by k8s/ingress.yaml for cert-manager Let's Encrypt.
# Set your domain and a valid email for certificate renewal.
PLATFORM_DOMAIN=platform.yourdomain.com
LETSENCRYPT_EMAIL=admin@yourdomain.com

# ── Observability (optional) ─────────────────────────────────────
# Prometheus metrics endpoint (if metrics-server is installed).
METRICS_ENABLED=false
